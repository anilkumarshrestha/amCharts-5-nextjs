export default nextConfig;

/** @type {import('next').NextConfig} */
const nextConfig = {
  basePath: "/amCharts-5-nextjs",
  output:'export',
  images: {
        unoptimized: true
    },

};

export default nextConfig;
